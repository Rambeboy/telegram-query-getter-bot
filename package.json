{
  "name": "telegram-query-getter-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "Telegram web apps query getter, to get query data provided by telegram when authenticating to Telegram Web Apps.",
  "main": "index.js",
  "scripts": {
    "start": "node scripts/start.js",
    "setup": "node scripts/setup.js",
    "dev": "node app/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "javascript-obfuscator app --output dist --config build-config.json"
  },
  "keywords": [
    "telegram-query-getter" 
    "query-web",
    "nodejs",
    "getter-query",
    "sessions"
  ],
  "author": "Nofan Rambe",
  "license": "MIT",
  "dependencies": {
    "input": "^1.0.1",
    "telegram": "^2.22.2",
    "winston": "^3.13.0"
  }
}
